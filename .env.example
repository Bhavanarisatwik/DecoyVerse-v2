# API Configuration

# Express backend (auth + vault) - runs on port 5000
VITE_EXPRESS_API_URL=http://localhost:5000/api

# FastAPI backend (nodes, decoys, alerts, logs, ai-insights) - runs on port 8001
VITE_FASTAPI_API_URL=http://localhost:8001
