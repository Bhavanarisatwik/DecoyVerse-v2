# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/decay

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------
# Email — SendGrid API (https://sendgrid.com — free tier: 100 emails/day)
# No domain verification needed — just verify a single sender email:
# sendgrid.com → Settings → Sender Authentication → Single Sender Verification
# SENDGRID_FROM must exactly match your verified sender email/name.
# -----------------------------------------------------------
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxx
SENDGRID_FROM=DecoyVerse Security <your-verified-email@gmail.com>

# -----------------------------------------------------------
# Internal service-to-service secret (Python FastAPI → Express)
# Must match INTERNAL_SECRET in the Python backend .env
# -----------------------------------------------------------
INTERNAL_SECRET=change_this_to_a_random_string
