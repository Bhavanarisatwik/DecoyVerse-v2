# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/decay

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------
# Email — Resend API (https://resend.com — free tier: 3k emails/month)
# Cloud providers block SMTP ports, so direct Gmail never works on Render.
# Get a free API key at resend.com.
# RESEND_FROM: use "onboarding@resend.dev" on free tier (no domain needed),
#              or "DecoyVerse <alerts@yourdomain.com>" after verifying your domain.
# -----------------------------------------------------------
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
RESEND_FROM=DecoyVerse Security <onboarding@resend.dev>

# -----------------------------------------------------------
# Internal service-to-service secret (Python FastAPI → Express)
# Must match INTERNAL_SECRET in the Python backend .env
# -----------------------------------------------------------
INTERNAL_SECRET=change_this_to_a_random_string
